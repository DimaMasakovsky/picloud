import { UrlParentesisPipe } from './url-parentesis.pipe';

describe('UrlParentesisPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlParentesisPipe();

    expect(pipe).toBeTruthy();
  });
});
